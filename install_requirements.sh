# Instala aquí los requerimientos del sistema o Python necesarios para OpenTopoData.
# Ejemplo para Python:
# pip install -r requirements.txt

echo "Instalando requerimientos del sistema..."
# apt-get update && apt-get install -y <paquetes>

echo "Instalando requerimientos de Python..."
# pip install -r requirements.txt

echo "Requerimientos instalados."
